{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "_comment": "React Router v7 framework deploy on Vercel. Static assets come from the React Router build output; all SSR + /api routes are handled by Vercel Functions generated by the React Router + Vercel preset.",
  "framework": "react-router"
}
