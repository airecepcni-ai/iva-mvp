## Deploy note: UI flag + domain

- Set `NEXT_PUBLIC_UI_VERSION=v2` in Vercel for Preview and Production.
- Redeploy after changing env vars.
- Custom domain `ivaai.cz` should show "Valid" in Vercel Domains before it serves the latest build.
